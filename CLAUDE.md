# Claude Code Instructions

@AGENTS.md

This repository is the canonical offline ASPDotNetStorefront (ASPDNSF) 10 developer manual used for RPS ecommerce-store work. The full agent rules live in `AGENTS.md` (imported above) — follow them exactly. In short: before recommending or implementing any RPS store change, grep the manual's `*.htm` pages for the built-in token, AppConfig, XML package hook, event handler, or admin feature the platform already provides, and never invent platform capabilities.
